import React from "react";
//import { useHistory } from "react-router";

const Header2 = () => {
  //const history = useHistory();
  return (
    <nav className="navbar">
      <button className=" btn navbar-brand ms-5 navbarBrandButton">
        <b>
          <i>ShopCart</i>
        </b>
      </button>
    </nav>
  );
};

export default Header2;
